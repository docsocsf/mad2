{
  "scripts": {
    "start": "npx concurrently \"cd backend && yarn start:prod\" \"cd frontend && yarn start\"",
    "start:debug": "concurrently \" cd backend && yarn start:debug\" \"cd frontend && yarn start\""
  },
  "devDependencies": {
    "concurrently": "^5.2.0"
  }
}
